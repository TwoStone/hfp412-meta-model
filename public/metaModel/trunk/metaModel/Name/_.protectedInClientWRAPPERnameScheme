

    public void setIcon(IconRenderer renderer) {
        //TODO: Implement setIcon in wrapper class of association nameScheme in class Name
    	this.getWrappedObject().setIcon(renderer);
    }
	
	public String toString(){
    	//TODO: Implement toString in wrapper class for association nameScheme in class Name
    	return this.getWrappedObject().toString();
    }

    