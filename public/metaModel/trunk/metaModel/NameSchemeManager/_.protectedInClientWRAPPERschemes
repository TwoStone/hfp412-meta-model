

    public void setIcon(IconRenderer renderer) {
        //TODO: Implement setIcon in wrapper class of association schemes in class NameSchemeManager
    	this.getWrappedObject().setIcon(renderer);
    }
	
	public String toString(){
    	//TODO: Implement toString in wrapper class for association schemes in class NameSchemeManager
    	return this.getWrappedObject().toString();
    }

    